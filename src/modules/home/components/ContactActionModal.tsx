import { useGetContactsQuery } from "../api/queryEndpoints";
import { IContact } from "../api/types";

interface IProps {}

const ContactActionModal = ({}: IProps) => {
  return <></>;
};

export default ContactActionModal;

